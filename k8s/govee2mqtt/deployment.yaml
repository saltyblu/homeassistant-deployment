apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: govee2mqtt
  name: govee2mqtt
  namespace: homeassistant
spec:
  replicas: 1
  selector:
    matchLabels:
      app: govee2mqtt
  template:
    metadata:
      labels:
        app: govee2mqtt
    spec:
      containers:
        - image: ghcr.io/wez/govee2mqtt:latest # {"$imagepolicy": "homeassistant:govee2mqtt"}
          name: govee2mqtt
          env:
            - name: GOVEE_PASSWORD
              value-from:
                secretKeyRef:
                  name: govee2mqtt-secret
                  key: password
            - name: GOVEE_EMAIL
              value-from:
                secretKeyRef:
                  name: govee2mqtt-secret
                  key: email
            - name: GOVEE_MQTT_HOST
              value: mosquitto
            - name: GOVEE_TEMPERATURE_SCALE
              value: C
            - name: GOVEE_MQTT_PORT
              value: "1883"
            - name: TZ
              value: Europe/Berlin
          securityContext:
            privileged: true